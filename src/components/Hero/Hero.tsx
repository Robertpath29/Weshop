import React from "react";
import { HeroStyle } from "./hero.style";
import Header from "../Header/Header";

const Hero = () => {
    return (
        <HeroStyle>
            <Header />
        </HeroStyle>
    );
};

export default Hero;
